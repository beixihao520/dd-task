## 🖥️ Web UI (Optional)

We provide an optional Streamlit UI for interactive use:

```bash
# Install UI dependencies
pip install -r ui/requirements-ui.txt

# Run the UI
# without interaction
strealit run ui/old_app.py
# with interaction
streamlit run ui/app.py